// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey } from 'metal-orm';

@Entity({ tableName: 'publicacao' })
export class Publicacao {
  /**
   * identificador da publicação
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Ano da publicação
   */
  @Column(col.int())
  anoPublicacao?: number;

  /**
   * Código incremental interno da publicação
   */
  @Column(col.bigint())
  codPublicacao?: number;

  /**
   * Edição do diário
   */
  @Column(col.int())
  edicaoDiario?: number;

  /**
   * Descrição (nome) do diário
   */
  @Column(col.varchar(255))
  descricaoDiario?: string;

  /**
   * Página inicial da publicação no diário
   */
  @Column(col.int())
  paginaInicial?: number;

  /**
   * Página final da publicação no diário
   */
  @Column(col.int())
  paginaFinal?: number;

  /**
   * Data da publicação
   */
  @Column(col.varchar(255))
  dataPublicacao?: string;

  /**
   * Data da disponibilização do diário
   */
  @Column(col.varchar(255))
  dataDivulgacao?: string;

  /**
   * Data da criação da publicação em nossa base
   */
  @Column(col.varchar(255))
  dataCadastro?: string;

  /**
   * Número do processo da publicação
   */
  @Column(col.varchar(255))
  numeroProcesso?: string;

  /**
   * UF da publicação
   */
  @Column(col.varchar(255))
  ufPublicacao?: string;

  /**
   * cidade da publicação
   */
  @Column(col.varchar(255))
  cidadePublicacao?: string;

  /**
   * orgão da publicação
   */
  @Column(col.varchar(255))
  orgaoDescricao?: string;

  /**
   * Vara da publicação
   */
  @Column(col.text())
  varaDescricao?: string;

  /**
   * Corpo da Publicação
   */
  @Column(col.text())
  despachoPublicacao?: string;

  /**
   * Corpo da publicação
   */
  @Column(col.text())
  processoPublicacao?: string;

  /**
   * Indica se a publicação sofreu alguma correção (1) ou não (0 - padrão)
   */
  @Column(col.int())
  publicacaoCorrigida?: number;

  /**
   * Código do vínculo em nossa base
   */
  @Column(col.int())
  codVinculo?: number;

  /**
   * Nome do vínculo (advogado) da publicação
   */
  @Column(col.varchar(255))
  nomeVinculo?: string;

  /**
   * Número da OAB do vínculo (advogado)
   */
  @Column(col.int())
  OABNumero?: number;

  /**
   * Estado da OAB do vínculo (advogado)
   */
  @Column(col.varchar(255))
  OABEstado?: string;

  /**
   * Nome cadastrado no sistema
   */
  @Column(col.varchar(255))
  identificacaoCadastro?: string;

  /**
   * Código de integração da sua base em nossa base
   */
  @Column(col.varchar(255))
  codIntegracao?: string;

  /**
   * Complemento da publicação
   */
  @Column(col.varchar(255))
  complemento1?: string;

  /**
   * Descrição do complemento do orgão
   */
  @Column(col.varchar(255))
  orgaoDescricaoComp?: string;

  /**
   * Sigla wj
   */
  @Column(col.varchar(255))
  diarioSiglaWj?: string;

  /**
   * Indica se a publicação já foi marcada como exportada (1) ou não (0) (vide método setPublicacoes)
   */
  @Column(col.int())
  publicacaoExportada?: number;

  /**
   * Indica o grupo ao qual a publicação pertence (no caso do cliente possuir vários grupos de publicações)
   */
  @Column(col.int())
  codGrupo?: number;

  /**
   * Refere-se a uma publcação que foi descartada
   */
  @Column(col.boolean())
  descartada?: boolean;

  /**
   * Identificador da comunicação
   */
  @Column(col.int())
  comunicacao_id?: number;

  /**
   * Criado
   */
  @Column(col.notNull(col.datetime<Date>()))
  created!: Date;

  /**
   * Modificado
   */
  @Column(col.notNull(col.datetime<Date>()))
  modified!: Date;

  /**
   * Situação na qual se encontra a publicação
   */
  @Column(col.varchar(255))
  situacao?: string;

}
