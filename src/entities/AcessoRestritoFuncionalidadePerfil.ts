// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { AcessoRestritoFuncionalidade } from './AcessoRestritoFuncionalidade';
import { Perfil } from './Perfil';

@Entity({ tableName: 'acesso_restrito_funcionalidade_perfil' })
export class AcessoRestritoFuncionalidadePerfil {
  /**
   * Identificador único da relação das tabelas, acesso restrito, funcionalidades e perfil.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Informa a funcionalidade que é autorizada pelo perfil de usuários vinculado.
   * @remarks References dbo.acesso_restrito_funcionalidade.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'acesso_restrito_funcionalidade', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  acesso_restrito_funcionalidade_id!: number;

  /**
   * Identificador do perfil de grupo de usuários.
   * @remarks References dbo.perfil.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'perfil', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  perfil_id!: number;

  @BelongsTo({ target: () => AcessoRestritoFuncionalidade, foreignKey: 'acesso_restrito_funcionalidade_id' })
  acessoRestritoFuncionalidade!: BelongsToReference<AcessoRestritoFuncionalidade>;

  @BelongsTo({ target: () => Perfil, foreignKey: 'perfil_id' })
  perfil!: BelongsToReference<Perfil>;

}
