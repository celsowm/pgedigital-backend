// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { Acervo } from './Acervo';

@Entity({ tableName: 'mapeamento_acervo_sicaj' })
export class MapeamentoAcervoSicaj {
  /**
   * Identificador do mapeamento acervo sicaj
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena o nome de um acervo específico que está sendo relacionado ao Sistema de Controle de Acesso a Documentos Administrativos e Judiciais
   */
  @Column(col.notNull(col.text()))
  nome_acervo_sicaj!: string;

  /**
   * Armazena um nome corrigida, revisada ou atualizada para um acervo específico que está sendo relacionado ao Sistema de Controle de Acesso a Documentos Administrativos e Judiciais
   */
  @Column(col.text())
  nome_corrigido?: string;

  /**
   * Armazena o nome associada a um Acervo Documental (AD) específico que está sendo relacionado ao Sistema de Controle de Acesso a Documentos Administrativos e Judiciais
   */
  @Column(col.text())
  nome_ad?: string;

  /**
   * Identificador do usuário
   */
  @Column(col.int())
  usuario_id?: number;

  /**
   * Código do Servidor da Sicaj para mapeamento de Acervo
   */
  @Column(col.varchar(50))
  cod_servidor_sicaj?: string;

  /**
   * Identificador do acervo
   * @remarks References dbo.acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  acervo_id?: number;

  @BelongsTo({ target: () => Acervo, foreignKey: 'acervo_id' })
  acervo!: BelongsToReference<Acervo>;

}
