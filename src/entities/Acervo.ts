// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, ManyToManyCollection } from 'metal-orm';
import { AcervoClassificacao } from './AcervoClassificacao.js';
import { AcervoDestinatarioPa } from './AcervoDestinatarioPa.js';
import { AcervoEquipeApoio } from './AcervoEquipeApoio.js';
import { AcervoMateria } from './AcervoMateria.js';
import { AcervoTema } from './AcervoTema.js';
import { Classificacao } from './Classificacao.js';
import { Equipe } from './Equipe.js';
import { Especializada } from './Especializada.js';
import { FilaCircular } from './FilaCircular.js';
import { HistoricoDistribuicaoAcervo } from './HistoricoDistribuicaoAcervo.js';
import { MapeamentoAcervoSicaj } from './MapeamentoAcervoSicaj.js';
import { Materia } from './Materia.js';
import { Pessoa } from './Pessoa.js';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';
import { RaizCnpjAcervo } from './RaizCnpjAcervo.js';
import { Tema } from './Tema.js';
import { TipoAcervo } from './TipoAcervo.js';
import { TipoDivisaoCargaTrabalho } from './TipoDivisaoCargaTrabalho.js';
import { TipoMigracaoAcervo } from './TipoMigracaoAcervo.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'acervo' })
export class Acervo {
  /**
   * Identificador único do acervo. Usado para identificar e diferenciar cada entrada na tabela de forma exclusiva.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena o nome ou título do acervo em si, fornecendo uma identificação do acervo.
   */
  @Column(col.notNull(col.varchar(125)))
  nome!: string;

  /**
   * Identificador único da equipe responsável pelo acervo. Usado para identificar e diferenciar cada entrada na tabela de forma exclusiva.
   * @remarks References dbo.equipe.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'equipe', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  equipe_responsavel_id?: number;

  /**
   * Tipo de divisão de carga de trabalho: Padrão.
   * @remarks References dbo.tipo_divisao_carga_trabalho.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_divisao_carga_trabalho', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_divisao_carga_trabalho_id?: number;

  /**
   * identificador único que está relacionando o procurador titular associado a um registro específico na tabela.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  procurador_titular_id!: number;

  /**
   * Usada para vincular os itens a uma categoria ou área de especializada dentro do acervo.
   * @remarks References dbo.especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  especializada_id!: number;

  /**
   * Coluna usada para classificar os itens do acervo de acordo com suas diferentes categorias ou tipos. Ex: Acervo da Chefia/Assistente, Especializado, Núcleo e Regular.
   * @remarks References dbo.tipo_acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_acervo_id!: number;

  /**
   * Armazena um identificador ou código que está relacionado a uma fila circular específica no contexto do acervo.
   * @remarks References dbo.fila_circular.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'fila_circular', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  fila_circular_id?: number;

  /**
   * Identificador único que informa o tipo de migração do acervo.
   * @remarks References dbo.tipo_migracao_acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_migracao_acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_migracao_acervo_id?: number;

  /**
   * Informa se está em rotina.
   */
  @Column(col.notNull(col.boolean()))
  rotina_sob_demanda!: boolean;

  /**
   * armazena informações sobre o status de ativação ou disponibilidade do acervo.
   * @defaultValue ((1))
   */
  @Column(col.defaultRaw(col.notNull(col.int()), '((1))'))
  ativo!: number;

  /**
   * Armazena a data e hora da última modificação ou atualização de um registro específico no banco de dados. Útil para rastrear quando um registro foi alterado pela última vez.
   */
  @Column(col.datetime<Date>())
  modified?: Date;

  /**
   * Armazena a data e hora em que o registro específico foi criado ou adicionado à tabela.
   */
  @Column(col.datetime<Date>())
  created?: Date;

  @BelongsToMany({ target: () => Equipe, pivotTable: () => AcervoEquipeApoio, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'equipe_apoio_id' })
  equipes!: ManyToManyCollection<Equipe>;

  @BelongsToMany({ target: () => Usuario, pivotTable: () => AcervoDestinatarioPa, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'destinatario_pa_id' })
  usuarios!: ManyToManyCollection<Usuario>;

  @BelongsToMany({ target: () => Classificacao, pivotTable: () => AcervoClassificacao, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'classificacao_id' })
  classificacoes!: ManyToManyCollection<Classificacao>;

  @BelongsToMany({ target: () => Materia, pivotTable: () => AcervoMateria, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'materia_id' })
  materias!: ManyToManyCollection<Materia>;

  @BelongsToMany({ target: () => Tema, pivotTable: () => AcervoTema, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'tema_id' })
  temas!: ManyToManyCollection<Tema>;

  @BelongsToMany({ target: () => Pessoa, pivotTable: () => RaizCnpjAcervo, pivotForeignKeyToRoot: 'acervo_id', pivotForeignKeyToTarget: 'matriz_id' })
  pessoas!: ManyToManyCollection<Pessoa>;

  @HasMany({ target: () => AcervoEquipeApoio, foreignKey: 'acervo_id' })
  acervosEquipeApoio!: HasManyCollection<AcervoEquipeApoio>;

  @HasMany({ target: () => AcervoDestinatarioPa, foreignKey: 'acervo_id' })
  acervosDestinatarioPa!: HasManyCollection<AcervoDestinatarioPa>;

  @HasMany({ target: () => ProcessoAdministrativo, foreignKey: 'acervo_id' })
  processosAdministrativo!: HasManyCollection<ProcessoAdministrativo>;

  @HasMany({ target: () => HistoricoDistribuicaoAcervo, foreignKey: 'acervo_id' })
  historicosDistribuicaoAcervo!: HasManyCollection<HistoricoDistribuicaoAcervo>;

  @HasMany({ target: () => AcervoClassificacao, foreignKey: 'acervo_id' })
  acervosClassificacao!: HasManyCollection<AcervoClassificacao>;

  @HasMany({ target: () => AcervoMateria, foreignKey: 'acervo_id' })
  acervosMateria!: HasManyCollection<AcervoMateria>;

  @HasMany({ target: () => AcervoTema, foreignKey: 'acervo_id' })
  acervosTema!: HasManyCollection<AcervoTema>;

  @HasMany({ target: () => RaizCnpjAcervo, foreignKey: 'acervo_id' })
  raizesCnpjAcervo!: HasManyCollection<RaizCnpjAcervo>;

  @HasMany({ target: () => MapeamentoAcervoSicaj, foreignKey: 'acervo_id' })
  mapeamentosAcervoSicaj!: HasManyCollection<MapeamentoAcervoSicaj>;

  @BelongsTo({ target: () => Equipe, foreignKey: 'equipe_responsavel_id' })
  equipeResponsavel?: Equipe;

  @BelongsTo({ target: () => TipoDivisaoCargaTrabalho, foreignKey: 'tipo_divisao_carga_trabalho_id' })
  tipoDivisaoCargaTrabalho?: TipoDivisaoCargaTrabalho;

  @BelongsTo({ target: () => Usuario, foreignKey: 'procurador_titular_id' })
  procuradorTitular?: Usuario;

  @BelongsTo({ target: () => Especializada, foreignKey: 'especializada_id' })
  especializada?: Especializada;

  @BelongsTo({ target: () => TipoAcervo, foreignKey: 'tipo_acervo_id' })
  tipoAcervo?: TipoAcervo;

  @BelongsTo({ target: () => FilaCircular, foreignKey: 'fila_circular_id' })
  filaCircular?: FilaCircular;

  @BelongsTo({ target: () => TipoMigracaoAcervo, foreignKey: 'tipo_migracao_acervo_id' })
  tipoMigracaoAcervo?: TipoMigracaoAcervo;

}
