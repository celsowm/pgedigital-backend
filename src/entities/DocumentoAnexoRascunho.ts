// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { ModeloFormularioProcessoAdministrativo } from './ModeloFormularioProcessoAdministrativo.js';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';
import { ProvidenciaJuridica } from './ProvidenciaJuridica.js';
import { TipoDocumento } from './TipoDocumento.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'documento_anexo_rascunho' })
export class DocumentoAnexoRascunho {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena o nome específico do arquivo.
   */
  @Column(col.text())
  nome?: string;

  /**
   * Armazena o binário do documento em questão.
   */
  @Column(col.blob())
  binario?: Buffer;

  /**
   * descrição do tipo de conteúdo do arquivo, geralmente indicado por uma combinação de letras e números.
   */
  @Column(col.varchar(50))
  mimetype?: string;

  /**
   * Armazena uma breve descrição do arquivo.
   */
  @Column(col.text())
  descricao?: string;

  /**
   * Identificador do processo administrativo vinculado.
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id?: number;

  /**
   * Identificador que informa o documento rascunho vinculado.
   */
  @Column(col.int())
  documento_rascunho_id?: number;

  /**
   * Identificador que informa o usuário vinculado.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_id?: number;

  /**
   * Identificador que informa o tipo do documento.
   * @remarks References dbo.tipo_documento.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_documento', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_documento_id?: number;

  /**
   * Identificador que informa a providência jurídica vinculada.
   * @remarks References dbo.providencia_juridica.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'providencia_juridica', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  providencia_juridica_id?: number;

  /**
   * Armazena o tipo de extensão do documento.
   */
  @Column(col.varchar(5))
  extensao?: string;

  /**
   * Confere se foi realizada minuta do documento.
   */
  @Column(col.boolean())
  minuta_realizada?: boolean;

  /**
   * Confere se documento é original.
   */
  @Column(col.boolean())
  confere_original?: boolean;

  /**
   * Informa o tipo de documento em anexo.
   */
  @Column(col.varchar(10))
  tipo_documento_anexo?: string;

  /**
   * @remarks References dbo.modelo_formulario_processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'modelo_formulario_processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  modelo_formulario_processo_administrativo_id?: number;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo!: BelongsToReference<ProcessoAdministrativo>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_id' })
  usuario!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => TipoDocumento, foreignKey: 'tipo_documento_id' })
  tipoDocumento!: BelongsToReference<TipoDocumento>;

  @BelongsTo({ target: () => ProvidenciaJuridica, foreignKey: 'providencia_juridica_id' })
  providenciaJuridica!: BelongsToReference<ProvidenciaJuridica>;

  @BelongsTo({ target: () => ModeloFormularioProcessoAdministrativo, foreignKey: 'modelo_formulario_processo_administrativo_id' })
  modeloFormularioProcessoAdministrativo!: BelongsToReference<ModeloFormularioProcessoAdministrativo>;

}
