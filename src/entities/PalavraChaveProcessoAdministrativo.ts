// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { PalavraChave } from './PalavraChave';
import { ProcessoAdministrativo } from './ProcessoAdministrativo';

@Entity({ tableName: 'palavra_chave_processo_administrativo' })
export class PalavraChaveProcessoAdministrativo {
  /**
   * Identificador da Palavra Chave do Processo Administrativo
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da Palavra chave vinculada com o Processo Administrativo
   * @remarks References dbo.palavra_chave.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'palavra_chave', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  palavra_chave_id!: number;

  /**
   * Processo Administrativo vinculado
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id!: number;

  /**
   * Armazena o valor.
   */
  @Column(col.varchar(255))
  valor?: string;

  @BelongsTo({ target: () => PalavraChave, foreignKey: 'palavra_chave_id' })
  palavraChave!: BelongsToReference<PalavraChave>;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo!: BelongsToReference<ProcessoAdministrativo>;

}
