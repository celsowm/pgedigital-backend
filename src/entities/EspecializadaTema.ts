// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { Especializada } from './Especializada.js';
import { ExitoSucumbencia } from './ExitoSucumbencia.js';
import { FilaCircular } from './FilaCircular.js';
import { Tema } from './Tema.js';
import { TipoAcervo } from './TipoAcervo.js';

@Entity({ tableName: 'especializada_tema' })
export class EspecializadaTema {
  /**
   * Identificador do tema da especializada
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da especializada
   * @remarks References dbo.especializada.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'especializada', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  especializada_id!: number;

  /**
   * Identificador do tema
   * @remarks References dbo.tema.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tema', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  tema_id!: number;

  /**
   * Identificador do tipo do acervo
   * @remarks References dbo.tipo_acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_acervo_id?: number;

  /**
   * Indica se a alocação de um tema específico para análise e tratamento foi realizada automaticamente pelo sistema.
   */
  @Column(col.boolean())
  distribuicao_automatica?: boolean;

  /**
   * Dá o valor econômico da demanda
   */
  @Column(col.decimal(18, 2))
  valor_economico_demanda?: number;

  /**
   * Identificador do êxito sucumbência
   * @remarks References dbo.exito_sucumbencia.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'exito_sucumbencia', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  exito_sucumbencia_id?: number;

  /**
   * Identificador da fila circular
   * @defaultValue (NULL)
   * @remarks References dbo.fila_circular.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.defaultRaw(col.int(), '(NULL)'), { table: 'fila_circular', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  fila_circular_id?: number;

  @BelongsTo({ target: () => Especializada, foreignKey: 'especializada_id' })
  especializada!: BelongsToReference<Especializada>;

  @BelongsTo({ target: () => Tema, foreignKey: 'tema_id' })
  tema!: BelongsToReference<Tema>;

  @BelongsTo({ target: () => TipoAcervo, foreignKey: 'tipo_acervo_id' })
  tipoAcervo!: BelongsToReference<TipoAcervo>;

  @BelongsTo({ target: () => ExitoSucumbencia, foreignKey: 'exito_sucumbencia_id' })
  exitoSucumbencia!: BelongsToReference<ExitoSucumbencia>;

  @BelongsTo({ target: () => FilaCircular, foreignKey: 'fila_circular_id' })
  filaCircular!: BelongsToReference<FilaCircular>;

}
