// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { AfastamentoPessoaUsuario } from './AfastamentoPessoaUsuario';
import { FilaCircular } from './FilaCircular';
import { TipoAfastamento } from './TipoAfastamento';
import { TipoDivisaoCargaTrabalho } from './TipoDivisaoCargaTrabalho';
import { Usuario } from './Usuario';

@Entity({ tableName: 'afastamento_pessoa' })
export class AfastamentoPessoa {
  /**
   * Identificador único da tabela afastamento relacionada à pessoa.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Usuário em afastamento.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_id!: number;

  /**
   * Identificador do tipo de afastamento do usuário.
   * @remarks References dbo.tipo_afastamento.id (on delete NO ACTION, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_afastamento', column: 'id', onDelete: 'NO ACTION', onUpdate: 'CASCADE' }))
  tipo_afastamento_id!: number;

  /**
   * Data de início do afastamento.
   */
  @Column(col.notNull(col.date<Date>()))
  data_inicio!: Date;

  /**
   * Data final do afastamento.
   */
  @Column(col.notNull(col.date<Date>()))
  data_fim!: Date;

  /**
   * Informa a quantidade de dias antes de iniciar o afastamento da pessoa.
   */
  @Column(col.int())
  dias_antes_inicio?: number;

  /**
   * Informa a quantidade de dias antes de terminar o afastamento da pessoa.
   */
  @Column(col.int())
  dias_antes_fim?: number;

  /**
   * Identificador que informa o tipo da divisão de carga de trabalho.
   * @remarks References dbo.tipo_divisao_carga_trabalho.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_divisao_carga_trabalho', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_divisao_carga_trabalho_id!: number;

  /**
   * Informa a fila circular.
   * @remarks References dbo.fila_circular.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'fila_circular', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  fila_circular_id?: number;

  /**
   * Data de criação do pedido de afastamento.
   */
  @Column(col.datetime<Date>())
  data_criacao?: Date;

  /**
   * Data de início da comunicação do afastamento.
   */
  @Column(col.date<Date>())
  data_inicio_comunicacao?: Date;

  /**
   * Data final da comunicação do afastamento.
   */
  @Column(col.date<Date>())
  data_fim_comunicacao?: Date;

  /**
   * @defaultValue (getdate())
   */
  @Column(col.defaultRaw(col.datetime<Date>(), '(getdate())'))
  created?: Date;

  @Column(col.datetime<Date>())
  modified?: Date;

  @BelongsToMany({ target: () => Usuario, pivotTable: () => AfastamentoPessoaUsuario, pivotForeignKeyToRoot: 'afastamento_pessoa_id', pivotForeignKeyToTarget: 'usuario_id' })
  usuarios!: ManyToManyCollection<Usuario>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_id' })
  usuario!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => TipoAfastamento, foreignKey: 'tipo_afastamento_id' })
  tipoAfastamento!: BelongsToReference<TipoAfastamento>;

  @BelongsTo({ target: () => TipoDivisaoCargaTrabalho, foreignKey: 'tipo_divisao_carga_trabalho_id' })
  tipoDivisaoCargaTrabalho!: BelongsToReference<TipoDivisaoCargaTrabalho>;

  @BelongsTo({ target: () => FilaCircular, foreignKey: 'fila_circular_id' })
  filaCircular!: BelongsToReference<FilaCircular>;

  @HasMany({ target: () => AfastamentoPessoaUsuario, foreignKey: 'afastamento_pessoa_id' })
  afastamentoPessoaUsuarios!: HasManyCollection<AfastamentoPessoaUsuario>;

}
