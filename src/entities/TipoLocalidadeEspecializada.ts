// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { Especializada } from './Especializada';

@Entity({ tableName: 'tipo_localidade_especializada' })
export class TipoLocalidadeEspecializada {
  /**
   * identificador do tipo da localidade da especializada
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * nome do tipo da localidade da especializada. Ex:Sede
   */
  @Column(col.notNull(col.varchar(50)))
  nome!: string;

  @HasMany({ target: () => Especializada, foreignKey: 'tipo_localidade_especializada_id' })
  especializadas!: HasManyCollection<Especializada>;

}
