// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { EstadoModeloFormulario } from './EstadoModeloFormulario';
import { ModeloFormularioProcessoAdministrativo } from './ModeloFormularioProcessoAdministrativo';
import { Usuario } from './Usuario';

@Entity({ tableName: 'modelo_formulario_processo_administrativo_estado_modelo_formulario' })
export class ModeloFormularioProcessoAdministrativoEstadoModeloFormulario {
  /**
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * @remarks References dbo.modelo_formulario_processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'modelo_formulario_processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  modelo_formulario_processo_administrativo_id!: number;

  /**
   * @remarks References dbo.estado_modelo_formulario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'estado_modelo_formulario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  estado_modelo_formulario_id!: number;

  /**
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_id!: number;

  @Column(col.varchar(255))
  descricao?: string;

  @Column(col.notNull(col.datetime<Date>()))
  created!: Date;

  @BelongsTo({ target: () => ModeloFormularioProcessoAdministrativo, foreignKey: 'modelo_formulario_processo_administrativo_id' })
  modeloFormularioProcessoAdministrativo!: BelongsToReference<ModeloFormularioProcessoAdministrativo>;

  @BelongsTo({ target: () => EstadoModeloFormulario, foreignKey: 'estado_modelo_formulario_id' })
  estadoModeloFormulario!: BelongsToReference<EstadoModeloFormulario>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_id' })
  usuario!: BelongsToReference<Usuario>;

}
