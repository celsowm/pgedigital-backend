// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { CodigoExterno } from './CodigoExterno';
import { SistemaOrigem } from './SistemaOrigem';
import { TipoProcedimentoSei } from './TipoProcedimentoSei';

@Entity({ tableName: 'procedimento_sei' })
export class ProcedimentoSei {
  /**
   * Identificador do procedimento sei
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador do procedimento
   */
  @Column(col.notNull(col.int()))
  id_procedimento!: number;

  /**
   * Fluxo de trabalho executado pelo SEI
   */
  @Column(col.notNull(col.varchar(50)))
  procedimento_formatado!: string;

  /**
   * Detalhes que descreve como realizar uma tarefa
   */
  @Column(col.notNull(col.text()))
  especificacao!: string;

  /**
   * Data da última atuação no procedimento Sei
   */
  @Column(col.notNull(col.date<Date>()))
  data_autuacao!: Date;

  /**
   * URL para acessar o Procedimento Sei
   */
  @Column(col.notNull(col.varchar(255)))
  link_acesso!: string;

  /**
   * Nível de acesso na localidade
   */
  @Column(col.notNull(col.int()))
  nivel_acesso_local!: number;

  /**
   * Nível de acesso Global
   */
  @Column(col.notNull(col.int()))
  nivel_acesso_global!: number;

  /**
   * Armazena o identificador único do tipo do procedimento sei
   * @remarks References dbo.tipo_procedimento_sei.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_procedimento_sei', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_procedimento_sei_id!: number;

  /**
   * Data da última atualização do Procedimento Sei
   */
  @Column(col.datetime<Date>())
  data_atualizacao?: Date;

  @BelongsToMany({ target: () => SistemaOrigem, pivotTable: () => CodigoExterno, pivotForeignKeyToRoot: 'procedimento_sei_id', pivotForeignKeyToTarget: 'sistema_origem_id' })
  sistemaOrigems!: ManyToManyCollection<SistemaOrigem>;

  @HasMany({ target: () => CodigoExterno, foreignKey: 'procedimento_sei_id' })
  codigoExternos!: HasManyCollection<CodigoExterno>;

  @BelongsTo({ target: () => TipoProcedimentoSei, foreignKey: 'tipo_procedimento_sei_id' })
  tipoProcedimentoSei!: BelongsToReference<TipoProcedimentoSei>;

}
