// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasOne, BelongsTo, BelongsToMany, HasManyCollection, HasOneReference, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { Acervo } from './Acervo';
import { AssuntoLocal } from './AssuntoLocal';
import { ClassificacaoEspecializadaTema } from './ClassificacaoEspecializadaTema';
import { Confidencialidade } from './Confidencialidade';
import { ConfidencialidadeEspecializada } from './ConfidencialidadeEspecializada';
import { Equipe } from './Equipe';
import { EspecializadaAssistente } from './EspecializadaAssistente';
import { EspecializadaDistribuidor } from './EspecializadaDistribuidor';
import { EspecializadaMateria } from './EspecializadaMateria';
import { EspecializadaTema } from './EspecializadaTema';
import { EspecializadaUsuarioNotificacao } from './EspecializadaUsuarioNotificacao';
import { FilaCircular } from './FilaCircular';
import { HistoricoDistribuicaoEspecializada } from './HistoricoDistribuicaoEspecializada';
import { HistoricoUsuarioEspecializada } from './HistoricoUsuarioEspecializada';
import { Localidade } from './Localidade';
import { MapeamentoLocalidadeRegional } from './MapeamentoLocalidadeRegional';
import { Materia } from './Materia';
import { Modelo } from './Modelo';
import { ModeloFormulario } from './ModeloFormulario';
import { OrgaoJulgador } from './OrgaoJulgador';
import { PredicaoAssuntoEspecializada } from './PredicaoAssuntoEspecializada';
import { PredicaoEspecializadaMachineLearning } from './PredicaoEspecializadaMachineLearning';
import { PredicaoOrgaoJulgadorEspecializada } from './PredicaoOrgaoJulgadorEspecializada';
import { ProcessoAdministrativo } from './ProcessoAdministrativo';
import { ProcessoJudicial } from './ProcessoJudicial';
import { RegistroTramitacao } from './RegistroTramitacao';
import { SorteioCorreicao } from './SorteioCorreicao';
import { TipoDispensa } from './TipoDispensa';
import { TipoDivisaoCargaTrabalho } from './TipoDivisaoCargaTrabalho';
import { TipoEspecializada } from './TipoEspecializada';
import { TipoLocalidadeEspecializada } from './TipoLocalidadeEspecializada';
import { Usuario } from './Usuario';

@Entity({ tableName: 'especializada' })
export class Especializada {
  /**
   * Armazena um identificador único ou código para cada registro específico na tabela.
   * @remarks Auto-increment identity column; References dbo.especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @PrimaryKey(col.references(col.notNull(col.autoIncrement(col.int())), { table: 'especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  id!: number;

  /**
   * Armazena um identificador único ou o código referente a uma equipe de triagem específica.
   * @remarks References dbo.equipe.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'equipe', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  equipe_triagem_id?: number;

  /**
   * Armazena um código ou identificador específico do responsável da especializada.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  responsavel_id!: number;

  /**
   * Armazena o nome específico da especializada.
   */
  @Column(col.notNull(col.text()))
  nome!: string;

  /**
   * Identifica se é usuário ou não do PGE digital.
   */
  @Column(col.notNull(col.boolean()))
  usa_pge_digital!: boolean;

  /**
   * Código de identificação associado a uma ação ou processo administrativo.
   */
  @Column(col.notNull(col.int()))
  codigo_ad!: number;

  /**
   * Identifica se é usuário ou não do plantão audiência.
   */
  @Column(col.notNull(col.boolean()))
  usa_plantao_audiencia!: boolean;

  /**
   * Identificador do tipo de divisão da carga de trabalho.
   * @remarks References dbo.tipo_divisao_carga_trabalho.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_divisao_carga_trabalho', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_divisao_carga_trabalho_id?: number;

  /**
   * Armazena um código ou identificador único do tipo de localidade da especializada.
   * @remarks References dbo.tipo_localidade_especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_localidade_especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_localidade_especializada_id?: number;

  /**
   * Armazena o endereço de e-mail.
   */
  @Column(col.varchar(255))
  email?: string;

  /**
   * Refere a uma indicação de restrição ou limitação específica.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  restricao_ponto_focal!: boolean;

  /**
   * Identifica a especializada pela sigla em abreviação.
   */
  @Column(col.varchar(10))
  sigla?: string;

  /**
   * Armazena o código ou identificador único do tipo da especializada.
   * @remarks References dbo.tipo_especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_especializada_id?: number;

  /**
   * Refere a um indicador ou sinalizador associado ao processo de triagem realizado em casos ou processos específicos.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  especializada_triagem!: boolean;

  /**
   * Armazena o número máximo da caixa de entrada.
   */
  @Column(col.int())
  caixa_entrada_max?: number;

  @BelongsToMany({ target: () => OrgaoJulgador, pivotTable: () => PredicaoOrgaoJulgadorEspecializada, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'orgao_julgador_id' })
  orgaoJulgadors!: ManyToManyCollection<OrgaoJulgador>;

  @BelongsToMany({ target: () => AssuntoLocal, pivotTable: () => PredicaoAssuntoEspecializada, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'assunto_id' })
  assuntoLocals!: ManyToManyCollection<AssuntoLocal>;

  @BelongsToMany({ target: () => Materia, pivotTable: () => EspecializadaMateria, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'materia_id' })
  materias!: ManyToManyCollection<Materia>;

  @BelongsToMany({ target: () => Usuario, pivotTable: () => EspecializadaDistribuidor, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'distribuidor_id' })
  usuarios!: ManyToManyCollection<Usuario>;

  @BelongsToMany({ target: () => FilaCircular, pivotTable: () => Equipe, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'fila_circular_id' })
  filaCirculars!: ManyToManyCollection<FilaCircular>;

  @BelongsToMany({ target: () => Confidencialidade, pivotTable: () => ConfidencialidadeEspecializada, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'confidencialidade_id' })
  confidencialidades!: ManyToManyCollection<Confidencialidade>;

  @BelongsToMany({ target: () => Localidade, pivotTable: () => MapeamentoLocalidadeRegional, pivotForeignKeyToRoot: 'especializada_regional_id', pivotForeignKeyToTarget: 'localidade_id' })
  localidades!: ManyToManyCollection<Localidade>;

  @BelongsToMany({ target: () => ProcessoJudicial, pivotTable: () => PredicaoEspecializadaMachineLearning, pivotForeignKeyToRoot: 'especializada_id', pivotForeignKeyToTarget: 'processo_judicial_id' })
  processoJudicials!: ManyToManyCollection<ProcessoJudicial>;

  @HasMany({ target: () => PredicaoOrgaoJulgadorEspecializada, foreignKey: 'especializada_id' })
  predicaoOrgaoJulgadorEspecializadas!: HasManyCollection<PredicaoOrgaoJulgadorEspecializada>;

  @HasMany({ target: () => ClassificacaoEspecializadaTema, foreignKey: 'especializada_id' })
  classificacaoEspecializadaTemas!: HasManyCollection<ClassificacaoEspecializadaTema>;

  @HasMany({ target: () => PredicaoAssuntoEspecializada, foreignKey: 'especializada_id' })
  predicaoAssuntoEspecializadas!: HasManyCollection<PredicaoAssuntoEspecializada>;

  @HasMany({ target: () => EspecializadaMateria, foreignKey: 'especializada_id' })
  especializadaMaterias!: HasManyCollection<EspecializadaMateria>;

  @HasMany({ target: () => EspecializadaTema, foreignKey: 'especializada_id' })
  especializadaTemas!: HasManyCollection<EspecializadaTema>;

  @HasMany({ target: () => EspecializadaDistribuidor, foreignKey: 'especializada_id' })
  especializadaDistribuidors!: HasManyCollection<EspecializadaDistribuidor>;

  @HasMany({ target: () => Equipe, foreignKey: 'especializada_id' })
  equipes!: HasManyCollection<Equipe>;

  @HasMany({ target: () => TipoDispensa, foreignKey: 'especializada_id' })
  tipoDispensas!: HasManyCollection<TipoDispensa>;

  @HasMany({ target: () => ModeloFormulario, foreignKey: 'especializada_id' })
  modeloFormularios!: HasManyCollection<ModeloFormulario>;

  @HasMany({ target: () => EspecializadaAssistente, foreignKey: 'especializada_id' })
  especializadaAssistentes!: HasManyCollection<EspecializadaAssistente>;

  @BelongsTo({ target: () => Especializada, foreignKey: 'id' })
  dboEspecializada!: BelongsToReference<Especializada>;

  @HasOne({ target: () => Especializada, foreignKey: 'id' })
  especializada!: HasOneReference<Especializada>;

  @BelongsTo({ target: () => Equipe, foreignKey: 'equipe_triagem_id' })
  equipeTriagem!: BelongsToReference<Equipe>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'responsavel_id' })
  responsavel!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => TipoDivisaoCargaTrabalho, foreignKey: 'tipo_divisao_carga_trabalho_id' })
  tipoDivisaoCargaTrabalho!: BelongsToReference<TipoDivisaoCargaTrabalho>;

  @BelongsTo({ target: () => TipoLocalidadeEspecializada, foreignKey: 'tipo_localidade_especializada_id' })
  tipoLocalidadeEspecializada!: BelongsToReference<TipoLocalidadeEspecializada>;

  @BelongsTo({ target: () => TipoEspecializada, foreignKey: 'tipo_especializada_id' })
  tipoEspecializada!: BelongsToReference<TipoEspecializada>;

  @HasMany({ target: () => ProcessoAdministrativo, foreignKey: 'especializada_id' })
  processoAdministrativos!: HasManyCollection<ProcessoAdministrativo>;

  @HasMany({ target: () => HistoricoDistribuicaoEspecializada, foreignKey: 'especializada_id' })
  historicoDistribuicaoEspecializadas!: HasManyCollection<HistoricoDistribuicaoEspecializada>;

  @HasMany({ target: () => HistoricoUsuarioEspecializada, foreignKey: 'especializada_id' })
  historicoUsuarioEspecializadas!: HasManyCollection<HistoricoUsuarioEspecializada>;

  @HasMany({ target: () => SorteioCorreicao, foreignKey: 'especializada_id' })
  sorteioCorreicaos!: HasManyCollection<SorteioCorreicao>;

  @HasMany({ target: () => EspecializadaUsuarioNotificacao, foreignKey: 'especializada_id' })
  especializadaUsuarioNotificacaos!: HasManyCollection<EspecializadaUsuarioNotificacao>;

  @HasMany({ target: () => RegistroTramitacao, foreignKey: 'especializada_id' })
  registroTramitacaos!: HasManyCollection<RegistroTramitacao>;

  @HasMany({ target: () => ConfidencialidadeEspecializada, foreignKey: 'especializada_id' })
  confidencialidadeEspecializadas!: HasManyCollection<ConfidencialidadeEspecializada>;

  @HasMany({ target: () => Modelo, foreignKey: 'especializada_id' })
  modelos!: HasManyCollection<Modelo>;

  @HasMany({ target: () => MapeamentoLocalidadeRegional, foreignKey: 'especializada_regional_id' })
  mapeamentoLocalidadeRegionals!: HasManyCollection<MapeamentoLocalidadeRegional>;

  @HasMany({ target: () => PredicaoEspecializadaMachineLearning, foreignKey: 'especializada_id' })
  predicaoEspecializadaMachineLearnings!: HasManyCollection<PredicaoEspecializadaMachineLearning>;

  @HasMany({ target: () => Acervo, foreignKey: 'especializada_id' })
  acervos!: HasManyCollection<Acervo>;

}
