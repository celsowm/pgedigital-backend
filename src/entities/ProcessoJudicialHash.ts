// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { ProcessoJudicial } from './ProcessoJudicial';

@Entity({ tableName: 'processo_judicial_hash' })
export class ProcessoJudicialHash {
  /**
   * Identificador do processo judicial hash
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * identificador do processo judicial
   * @remarks References dbo.processo_judicial.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.unique(col.notNull(col.int())), { table: 'processo_judicial', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_judicial_id!: number;

  /**
   * Refere-se o cabeçalho do processo judicial
   */
  @Column(col.varchar(100))
  cabecalho?: string;

  /**
   * Refere-se as movimentações de um processo judicial
   */
  @Column(col.varchar(100))
  movimento?: string;

  /**
   * Refere-se ao documento do processo judicial
   */
  @Column(col.varchar(100))
  documento?: string;

  /**
   * A data de última atualização refere-se ao momento mais recente em que informações foram revisadas, alteradas ou adicionadas
   */
  @Column(col.datetime<Date>())
  data_ultima_atualizacao?: Date;

  @BelongsTo({ target: () => ProcessoJudicial, foreignKey: 'processo_judicial_id' })
  processoJudicial!: BelongsToReference<ProcessoJudicial>;

}
