// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey } from 'metal-orm';

@Entity({ tableName: 'relacionamento_processual' })
export class RelacionamentoProcessual {
  /**
   * Identificador do relacionamento processual
   */
  @PrimaryKey(col.notNull(col.varchar(2)))
  id!: string;

  /**
   * é uma relação coordenada, ou seja, não existe uma hierarquia entre os processos, significa que um processo processo está "dentro" do outro ou quem um processo
   * @defaultValue (\'descrição do relacionamento processual\')
   */
  @Column(col.defaultRaw(col.notNull(col.varchar(100)), '(\'descrição do relacionamento processual\')'))
  descricao!: string;

}
