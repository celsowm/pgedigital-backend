// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { DecisaoJudicial } from './DecisaoJudicial';

@Entity({ tableName: 'tipo_fase_judicial' })
export class TipoFaseJudicial {
  /**
   * Id da fase judicial
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Nome da fase judicial
   */
  @Column(col.notNull(col.varchar(50)))
  nome!: string;

  @HasMany({ target: () => DecisaoJudicial, foreignKey: 'tipo_fase_judicial_id' })
  decisaoJudicials!: HasManyCollection<DecisaoJudicial>;

}
