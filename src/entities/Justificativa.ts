// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { HistoricoPrazo } from './HistoricoPrazo';

@Entity({ tableName: 'justificativa' })
export class Justificativa {
  /**
   * Identificador da justificativa
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena uma explicação detalhada
   */
  @Column(col.notNull(col.text()))
  descricao!: string;

  /**
   * Descreve a natureza da ação ou evento que requer uma justificação
   */
  @Column(col.varchar(255))
  acao?: string;

  @HasMany({ target: () => HistoricoPrazo, foreignKey: 'justificativa_id' })
  historicosPrazos!: HasManyCollection<HistoricoPrazo>;

}
