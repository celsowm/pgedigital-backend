// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, ManyToManyCollection } from 'metal-orm';
import { ParteRepresentante } from './ParteRepresentante.js';
import { Pessoa } from './Pessoa.js';
import { ProcessoJudicial } from './ProcessoJudicial.js';
import { RepresentanteJudicial } from './RepresentanteJudicial.js';
import { TipoModalidadeRelacionamentoPessoal } from './TipoModalidadeRelacionamentoPessoal.js';
import { TipoPolo } from './TipoPolo.js';

@Entity({ tableName: 'parte' })
export class Parte {
  /**
   * Identificador da parte
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da pessoa
   * @remarks References dbo.pessoa.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'pessoa', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  pessoa_id?: number;

  /**
   * Identificador do processo judicial
   * @remarks References dbo.processo_judicial.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_judicial', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_judicial_id?: number;

  /**
   * Questões que afetam a sociedade sejam abordadas de forma justa e equitativa
   */
  @Column(col.text())
  interesse_publico?: string;

  /**
   * Assistência jurídica que fornece serviços jurídicos a indivíduos ou grupos
   */
  @Column(col.boolean())
  assistencia_judiciaria?: boolean;

  /**
   * Uma notificação oficial que está aguardando ação ou resolução
   */
  @Column(col.int())
  intimacao_pendente?: number;

  /**
   * Identificador do tipo modalidade relacionamento pessoal
   * @remarks References dbo.tipo_modalidade_relacionamento_pessoal.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.varchar(2), { table: 'tipo_modalidade_relacionamento_pessoal', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_modalidade_relacionamento_pessoal_id?: string;

  /**
   * Identificador do tipo polo
   * @remarks References dbo.tipo_polo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.varchar(2), { table: 'tipo_polo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_polo_id?: string;

  /**
   * A parte foi incluída no sistema de forma manual
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  inclusao_manual!: boolean;

  @BelongsToMany({ target: () => RepresentanteJudicial, pivotTable: () => ParteRepresentante, pivotForeignKeyToRoot: 'parte_id', pivotForeignKeyToTarget: 'representante_judicial_id' })
  representantesJudicial!: ManyToManyCollection<RepresentanteJudicial>;

  @BelongsTo({ target: () => Pessoa, foreignKey: 'pessoa_id' })
  pessoa?: Pessoa;

  @BelongsTo({ target: () => ProcessoJudicial, foreignKey: 'processo_judicial_id' })
  processoJudicial?: ProcessoJudicial;

  @BelongsTo({ target: () => TipoModalidadeRelacionamentoPessoal, foreignKey: 'tipo_modalidade_relacionamento_pessoal_id' })
  tipoModalidadeRelacionamentoPessoal?: TipoModalidadeRelacionamentoPessoal;

  @BelongsTo({ target: () => TipoPolo, foreignKey: 'tipo_polo_id' })
  tipoPolo?: TipoPolo;

  @HasMany({ target: () => ParteRepresentante, foreignKey: 'parte_id' })
  partesRepresentante!: HasManyCollection<ParteRepresentante>;

}
