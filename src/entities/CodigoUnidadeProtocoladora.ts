// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column } from 'metal-orm';

@Entity({ tableName: 'codigo_unidade_protocoladora' })
export class CodigoUnidadeProtocoladora {
  /**
   * Abreviação do nome da unidade.
   */
  @Column(col.varchar(255))
  Unidade?: string;

  /**
   * Descrição completa do nome da unidade.
   */
  @Column(col.varchar(255))
  Descrição?: string;

  /**
   * Código da unidade protocoladora.
   */
  @Column(col.varchar(255))
  Código?: string;

}
