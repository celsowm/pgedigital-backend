// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection } from 'metal-orm';
import { DecisaoJudicial } from './DecisaoJudicial.js';
import { Instancia } from './Instancia.js';
import { MniTribunal } from './MniTribunal.js';

@Entity({ tableName: 'instancia_mni_tribunal' })
export class InstanciaMniTribunal {
  /**
   * Identificador da instância mni tribunal
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da instância
   * @remarks References dbo.instancia.id (on delete NO ACTION, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'instancia', column: 'id', onDelete: 'NO ACTION', onUpdate: 'CASCADE' }))
  instancia_id!: number;

  /**
   * Identificador do mni tribunal
   * @remarks References dbo.mni_tribunal.id (on delete NO ACTION, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'mni_tribunal', column: 'id', onDelete: 'NO ACTION', onUpdate: 'CASCADE' }))
  mni_tribunal_id!: number;

  @HasMany({ target: () => DecisaoJudicial, foreignKey: 'instancia_mni_tribunal_id' })
  decisoesJudicial!: HasManyCollection<DecisaoJudicial>;

  @BelongsTo({ target: () => Instancia, foreignKey: 'instancia_id' })
  instancia?: Instancia;

  @BelongsTo({ target: () => MniTribunal, foreignKey: 'mni_tribunal_id' })
  mniTribunal?: MniTribunal;

}
