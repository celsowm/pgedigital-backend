// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { ProcessoVinculado } from './ProcessoVinculado.js';

@Entity({ tableName: 'tipo_modalidade_vinculacao_processual' })
export class TipoModalidadeVinculacaoProcessual {
  /**
   * sigla do tipo de modalidade de vinculação processual.Ex:AR(Ação Rescisória)
   */
  @PrimaryKey(col.notNull(col.varchar(2)))
  id!: string;

  /**
   * nome do tipo de modalidade de vinculação processual. Ex:Ação rescisória, dependência
   */
  @Column(col.notNull(col.varchar(255)))
  nome!: string;

  @HasMany({ target: () => ProcessoVinculado, foreignKey: 'tipo_modalidade_vinculacao_processual_id' })
  processosVinculados!: HasManyCollection<ProcessoVinculado>;

}
