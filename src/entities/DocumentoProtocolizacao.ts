// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { DocumentoProcessoAdministrativo } from './DocumentoProcessoAdministrativo';
import { Protocolizacao } from './Protocolizacao';
import { TipoDocumento } from './TipoDocumento';
import { TipoDocumentoMni } from './TipoDocumentoMni';

@Entity({ tableName: 'documento_protocolizacao' })
export class DocumentoProtocolizacao {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Nome do documento.
   */
  @Column(col.text())
  nome?: string;

  /**
   * Binário do documento.
   */
  @Column(col.blob())
  binario?: Buffer;

  /**
   * Descrição do tipo de conteúdo do arquivo, geralmente indicado por uma combinação de letras e números.
   */
  @Column(col.varchar(50))
  mimetype?: string;

  /**
   * Descrição do documento.
   */
  @Column(col.text())
  descricao?: string;

  /**
   * Protocolização vinculada.
   * @remarks References dbo.protocolizacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'protocolizacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  protocolizacao_id!: number;

  /**
   * Identificador do tipo do documento.
   * @remarks References dbo.tipo_documento.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_documento', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_documento_id?: number;

  /**
   * Informa a ordem.
   */
  @Column(col.int())
  ordem?: number;

  /**
   * Página inicial.
   */
  @Column(col.int())
  pagina_inicial?: number;

  /**
   * Página final.
   */
  @Column(col.int())
  pagina_final?: number;

  /**
   * Documento no processo administrativo.
   * @remarks References dbo.documento_processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'documento_processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  documento_processo_administrativo_id?: number;

  /**
   * Informa se está assinado.
   */
  @Column(col.boolean())
  assinado?: boolean;

  /**
   * Informa se a assinatura é inválida.
   */
  @Column(col.boolean())
  assinatura_invalida?: boolean;

  /**
   * Informa o tipo de documento MNI.
   * @remarks References dbo.tipo_documento_mni.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'tipo_documento_mni', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_documento_mni_id?: number;

  @BelongsTo({ target: () => Protocolizacao, foreignKey: 'protocolizacao_id' })
  protocolizacao!: BelongsToReference<Protocolizacao>;

  @BelongsTo({ target: () => TipoDocumento, foreignKey: 'tipo_documento_id' })
  tipoDocumento!: BelongsToReference<TipoDocumento>;

  @BelongsTo({ target: () => DocumentoProcessoAdministrativo, foreignKey: 'documento_processo_administrativo_id' })
  documentoProcessoAdministrativo!: BelongsToReference<DocumentoProcessoAdministrativo>;

  @BelongsTo({ target: () => TipoDocumentoMni, foreignKey: 'tipo_documento_mni_id' })
  tipoDocumentoMni!: BelongsToReference<TipoDocumentoMni>;

}
