// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo } from 'metal-orm';
import { Acervo } from './Acervo.js';
import { Materia } from './Materia.js';

@Entity({ tableName: 'acervo_materia' })
export class AcervoMateria {
  /**
   * Identificador único da relação da tabela de acervo com matéria.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Matéria relacionada ao acervo.
   * @remarks References dbo.materia.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'materia', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  materia_id!: number;

  /**
   * Informa se está em distribuição automática.
   */
  @Column(col.boolean())
  distribuicao_automatica?: boolean;

  /**
   * Acervo relacionado.
   * @remarks References dbo.acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  acervo_id!: number;

  @BelongsTo({ target: () => Materia, foreignKey: 'materia_id' })
  materia?: Materia;

  @BelongsTo({ target: () => Acervo, foreignKey: 'acervo_id' })
  acervo?: Acervo;

}
