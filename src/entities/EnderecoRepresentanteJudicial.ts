// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { RepresentanteJudicial } from './RepresentanteJudicial';

@Entity({ tableName: 'endereco_representante_judicial' })
export class EnderecoRepresentanteJudicial {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Representante judicial.
   * @remarks References dbo.representante_judicial.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'representante_judicial', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  representante_judicial_id?: number;

  /**
   * Logradouro do representante.
   */
  @Column(col.text())
  logradouro?: string;

  /**
   * Número residencial do representante.
   */
  @Column(col.text())
  numero?: string;

  /**
   * Complemento da residência do representante.
   */
  @Column(col.text())
  complemento?: string;

  /**
   * Bairro do representante.
   */
  @Column(col.text())
  bairro?: string;

  /**
   * Cidade do representante.
   */
  @Column(col.text())
  cidade?: string;

  /**
   * Estado do representante.
   */
  @Column(col.text())
  estado?: string;

  /**
   * País do representante.
   */
  @Column(col.text())
  pais?: string;

  /**
   * CEP do representante.
   */
  @Column(col.text())
  cep?: string;

  @BelongsTo({ target: () => RepresentanteJudicial, foreignKey: 'representante_judicial_id' })
  representanteJudicial!: BelongsToReference<RepresentanteJudicial>;

}
