// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { MniTribunal } from './MniTribunal';

@Entity({ tableName: 'contato_mni_tribunal' })
export class ContatoMniTribunal {
  /**
   * Armazena um identificador único ou código para cada registro específico na tabela.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Campo que faz registro ao nome do contato.
   */
  @Column(col.notNull(col.text()))
  nome!: string;

  /**
   * Campo que faz registro ao email do contato.
   */
  @Column(col.notNull(col.varchar(50)))
  email!: string;

  /**
   * Campo que faz registro ao telefone do contato.
   */
  @Column(col.varchar(50))
  telefone?: string;

  /**
   * Armazena um identificador único ou código para cada registro de tribunal.
   * @remarks References dbo.mni_tribunal.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'mni_tribunal', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  mni_tribunal_id!: number;

  /**
   * Data em que o registro é inserido na tabela.
   */
  @Column(col.datetime<Date>())
  created?: Date;

  /**
   * Data em que o registro da tabela é modificado.
   */
  @Column(col.datetime<Date>())
  modified?: Date;

  @BelongsTo({ target: () => MniTribunal, foreignKey: 'mni_tribunal_id' })
  mniTribunal!: BelongsToReference<MniTribunal>;

}
