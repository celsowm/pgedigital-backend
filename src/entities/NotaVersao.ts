// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey } from 'metal-orm';

@Entity({ tableName: 'nota_versao' })
export class NotaVersao {
  /**
   * Identificador da nota versão
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Data da criação da nota versão
   */
  @Column(col.notNull(col.date<Date>()))
  data!: Date;

  /**
   * Número da Sprint da versão em nota
   */
  @Column(col.notNull(col.int()))
  sprint!: number;

  /**
   * Versão está ativa ou inativa
   */
  @Column(col.notNull(col.boolean()))
  ativo!: boolean;

  /**
   * Informação da nota
   */
  @Column(col.notNull(col.text()))
  mensagem!: string;

  /**
   * Data da exclusão
   */
  @Column(col.datetime<Date>())
  data_exclusao?: Date;

  /**
   * Data da inativação
   */
  @Column(col.datetime<Date>())
  data_inativacao?: Date;

}
