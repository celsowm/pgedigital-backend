// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { Carga } from './Carga';
import { Despacho } from './Despacho';
import { DocumentoRascunho } from './DocumentoRascunho';
import { Especializada } from './Especializada';
import { EstadoSolicitacao } from './EstadoSolicitacao';
import { Manifestacao } from './Manifestacao';
import { ProcessoAdministrativo } from './ProcessoAdministrativo';
import { Solicitacao } from './Solicitacao';
import { Tramitacao } from './Tramitacao';
import { Usuario } from './Usuario';

@Entity({ tableName: 'registro_tramitacao' })
export class RegistroTramitacao {
  /**
   * identificador da tramitação
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Id da tramitação
   * @remarks References dbo.tramitacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tramitacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tramitacao_id!: number;

  /**
   * Id do remetente
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  remetente_id!: number;

  /**
   * Id do destinatário
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  destinatario_id!: number;

  /**
   * Id do processo administrativo
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id!: number;

  /**
   * Trocar uma coisa por outra, muitas vezes de maneira que a segunda coisa assuma a função ou o papel da primeira
   */
  @Column(col.boolean())
  substituicao?: boolean;

  /**
   * Refere-se ao registro da data e hora em que um item, documento ou processo foi movido
   */
  @Column(col.datetime<Date>())
  data_hora_tramitacao?: Date;

  /**
   * Descrição dsobre os registros de tramitação
   */
  @Column(col.text())
  descricao_tramitacao?: string;

  /**
   * Id da especializada
   * @remarks References dbo.especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  especializada_id?: number;

  /**
   * Id da solitação
   * @remarks References dbo.solicitacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'solicitacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  solicitacao_id?: number;

  /**
   * Indica que um processo, ação ou sistema resultou na criação ou produção de um documento específico.
   */
  @Column(col.boolean())
  gerou_documento?: boolean;

  /**
   * Id do estado no qual está a solicitação
   * @remarks References dbo.estado_solicitacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'estado_solicitacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  estado_solicitacao_id?: number;

  /**
   * Identificador do documento rascunho
   * @remarks References dbo.documento_rascunho.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'documento_rascunho', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  documento_rascunho_id?: number;

  /**
   * Refere-se ao id do afastamento
   */
  @Column(col.int())
  afastamento_id?: number;

  @BelongsToMany({ target: () => Usuario, pivotTable: () => Despacho, pivotForeignKeyToRoot: 'registro_tramitacao_id', pivotForeignKeyToTarget: 'usuario_id' })
  usuarios!: ManyToManyCollection<Usuario>;

  @HasMany({ target: () => Manifestacao, foreignKey: 'registro_tramitacao_id' })
  manifestacaos!: HasManyCollection<Manifestacao>;

  @HasMany({ target: () => Carga, foreignKey: 'registro_tramitacao_id' })
  cargas!: HasManyCollection<Carga>;

  @BelongsTo({ target: () => Tramitacao, foreignKey: 'tramitacao_id' })
  tramitacao!: BelongsToReference<Tramitacao>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'remetente_id' })
  remetente!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'destinatario_id' })
  destinatario!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo!: BelongsToReference<ProcessoAdministrativo>;

  @BelongsTo({ target: () => Especializada, foreignKey: 'especializada_id' })
  especializada!: BelongsToReference<Especializada>;

  @BelongsTo({ target: () => Solicitacao, foreignKey: 'solicitacao_id' })
  solicitacao!: BelongsToReference<Solicitacao>;

  @BelongsTo({ target: () => EstadoSolicitacao, foreignKey: 'estado_solicitacao_id' })
  estadoSolicitacao!: BelongsToReference<EstadoSolicitacao>;

  @BelongsTo({ target: () => DocumentoRascunho, foreignKey: 'documento_rascunho_id' })
  documentoRascunho!: BelongsToReference<DocumentoRascunho>;

}
