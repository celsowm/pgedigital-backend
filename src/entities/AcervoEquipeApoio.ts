// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { Acervo } from './Acervo';
import { Equipe } from './Equipe';

@Entity({ tableName: 'acervo_equipe_apoio' })
export class AcervoEquipeApoio {
  /**
   * Armazena um identificador único para cada registro na tabela.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena um identificador ou código que está relacionado a uma equipe de apoio específica.
   * @remarks References dbo.equipe.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'equipe', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  equipe_apoio_id!: number;

  /**
   * Armazena um identificador ou código relacionado a itens específicos no acervo, coluna usada para associar informações sobre a equipe de apoio a registros na tabela.
   * @remarks References dbo.acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  acervo_id!: number;

  @BelongsTo({ target: () => Equipe, foreignKey: 'equipe_apoio_id' })
  equipeApoio!: BelongsToReference<Equipe>;

  @BelongsTo({ target: () => Acervo, foreignKey: 'acervo_id' })
  acervo!: BelongsToReference<Acervo>;

}
