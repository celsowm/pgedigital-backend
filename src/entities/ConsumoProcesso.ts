// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey } from 'metal-orm';

@Entity()
export class ConsumoProcesso {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Informa o número do processo.
   */
  @Column(col.notNull(col.varchar(20)))
  numero_processo!: string;

  /**
   * Verifica se o processo inclui movimentações.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  incluir_movimentacoes!: boolean;

  /**
   * Verifica se o processo inclui documentos.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  incluir_documentos!: boolean;

  /**
   * Informa se houve erro.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  erro!: boolean;

  /**
   * Informa se houve sucesso.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  sucesso!: boolean;

  /**
   * Data de inclusão.
   * @defaultValue (getdate())
   */
  @Column(col.defaultRaw(col.notNull(col.datetime<Date>()), '(getdate())'))
  data_inclusao!: Date;

  /**
   * Data do consumo do processo.
   */
  @Column(col.datetime<Date>())
  data_consumo?: Date;

  /**
   * Informa se o processo será atualizado.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  atualizar_processo!: boolean;

  /**
   * Arquiva a mensagem retornada.
   */
  @Column(col.text())
  mensagem?: string;

}
