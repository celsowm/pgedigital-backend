// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { ProcedimentoSei } from './ProcedimentoSei.js';

@Entity({ tableName: 'tipo_procedimento_sei' })
export class TipoProcedimentoSei {
  /**
   * identificador do tipo de procedimento do SEI
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * identificador do tipo de procedimento
   */
  @Column(col.notNull(col.int()))
  id_tipo_procedimento!: number;

  /**
   * nome do tipo de procedimento do SEI
   */
  @Column(col.notNull(col.text()))
  nome!: string;

  @HasMany({ target: () => ProcedimentoSei, foreignKey: 'tipo_procedimento_sei_id' })
  procedimentosSeis!: HasManyCollection<ProcedimentoSei>;

}
