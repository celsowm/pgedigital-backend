// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection, BelongsToReference } from 'metal-orm';
import { DocumentoProcessoAdministrativoAcordo } from './DocumentoProcessoAdministrativoAcordo';
import { Manifestacao } from './Manifestacao';
import { ProcessoAdministrativo } from './ProcessoAdministrativo';
import { Usuario } from './Usuario';

@Entity({ tableName: 'acordo' })
export class Acordo {
  /**
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  @Column(col.unique(col.notNull(col.varchar(10))))
  protocolo!: string;

  /**
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id?: number;

  /**
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  mediador_id?: number;

  @Column(col.notNull(col.datetime<Date>()))
  data_abertura!: Date;

  @Column(col.notNull(col.datetime<Date>()))
  created!: Date;

  @Column(col.notNull(col.datetime<Date>()))
  modified!: Date;

  @Column(col.varchar(20))
  cnj?: string;

  /**
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_contencioso_id?: number;

  /**
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_autocomposicao_id?: number;

  /**
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.int(), '((0))'))
  estado_arrolado?: number;

  @HasMany({ target: () => DocumentoProcessoAdministrativoAcordo, foreignKey: 'acordo_id' })
  documentoProcessoAdministrativoAcordos!: HasManyCollection<DocumentoProcessoAdministrativoAcordo>;

  @HasMany({ target: () => Manifestacao, foreignKey: 'acordo_id' })
  manifestacaos!: HasManyCollection<Manifestacao>;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo!: BelongsToReference<ProcessoAdministrativo>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'mediador_id' })
  mediador!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_contencioso_id' })
  processoContencioso!: BelongsToReference<ProcessoAdministrativo>;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_autocomposicao_id' })
  processoAutocomposicao!: BelongsToReference<ProcessoAdministrativo>;

}
