// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo } from 'metal-orm';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';
import { TipoAudiencia } from './TipoAudiencia.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'audiencia' })
export class Audiencia {
  /**
   * Armazena o código ou identificador único para cada registro na tabela.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador do processo administrativo vinculado à audiência.
   * @remarks References dbo.processo_administrativo.id (on delete CASCADE, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'processo_administrativo', column: 'id', onDelete: 'CASCADE', onUpdate: 'NO ACTION' }))
  processo_administrativo_id!: number;

  /**
   * Informa a data e hora da audiência.
   */
  @Column(col.notNull(col.datetime<Date>()))
  data_hora!: Date;

  /**
   * Identificador que informa o tipo de audiência.
   * @remarks References dbo.tipo_audiencia.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_audiencia', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_audiencia_id!: number;

  /**
   * Informa a vara que está vinculada à audiência.
   */
  @Column(col.notNull(col.varchar(255)))
  vara!: string;

  /**
   * Informa se audiência é presencial.
   * @defaultValue ((1))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((1))'))
  presencial!: boolean;

  /**
   * Armazena o link informado que ocorrerá a audiência.
   */
  @Column(col.varchar(255))
  link?: string;

  /**
   * Armazena o código ou um identificador único do usuário que criou a audiência.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_criador_id!: number;

  /**
   * Armazena o identificador único ou código que informa o usuário audiencista.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_audiencista_id?: number;

  /**
   * Informa as observações em relação à audiência.
   */
  @Column(col.varchar(255))
  observacoes?: string;

  /**
   * Informa se audiência está/foi cancelada.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  cancelada!: boolean;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo?: ProcessoAdministrativo;

  @BelongsTo({ target: () => TipoAudiencia, foreignKey: 'tipo_audiencia_id' })
  tipoAudiencia?: TipoAudiencia;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_criador_id' })
  usuarioCriador?: Usuario;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_audiencista_id' })
  usuarioAudiencista?: Usuario;

}
