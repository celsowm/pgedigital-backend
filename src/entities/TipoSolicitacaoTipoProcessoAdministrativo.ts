// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { TipoProcessoAdministrativo } from './TipoProcessoAdministrativo';
import { TipoSolicitacao } from './TipoSolicitacao';

@Entity({ tableName: 'tipo_solicitacao_tipo_processo_administrativo' })
export class TipoSolicitacaoTipoProcessoAdministrativo {
  /**
   * indentificador das relações entre tipo de solicitação e tipo de processo administrativo
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * chave estrangeira que aponta para o tipo de solicitação
   * @remarks References dbo.tipo_solicitacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_solicitacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_solicitacao_id!: number;

  /**
   * chave estrangeira que aponta para o tipo de processo administrativo
   * @remarks References dbo.tipo_processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tipo_processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_processo_administrativo_id!: number;

  @BelongsTo({ target: () => TipoSolicitacao, foreignKey: 'tipo_solicitacao_id' })
  tipoSolicitacao!: BelongsToReference<TipoSolicitacao>;

  @BelongsTo({ target: () => TipoProcessoAdministrativo, foreignKey: 'tipo_processo_administrativo_id' })
  tipoProcessoAdministrativo!: BelongsToReference<TipoProcessoAdministrativo>;

}
