// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { Correicao } from './Correicao';
import { Quesito } from './Quesito';

@Entity({ tableName: 'correicao_quesito' })
export class CorreicaoQuesito {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Quesito.
   * @remarks References dbo.quesito.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'quesito', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  quesito_id!: number;

  /**
   * Identificador que informa a correição vinculada.
   * @remarks References dbo.correicao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'correicao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  correicao_id!: number;

  @BelongsTo({ target: () => Quesito, foreignKey: 'quesito_id' })
  quesito!: BelongsToReference<Quesito>;

  @BelongsTo({ target: () => Correicao, foreignKey: 'correicao_id' })
  correicao!: BelongsToReference<Correicao>;

}
