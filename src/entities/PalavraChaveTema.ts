// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { PalavraChave } from './PalavraChave.js';
import { Tema } from './Tema.js';

@Entity({ tableName: 'palavra_chave_tema' })
export class PalavraChaveTema {
  /**
   * Identificador palavra-chave tema
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da palavra-chave
   * @remarks References dbo.palavra_chave.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'palavra_chave', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  palavra_chave_id!: number;

  /**
   * Identificador do tema
   * @remarks References dbo.tema.id (on delete CASCADE, on update CASCADE)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tema', column: 'id', onDelete: 'CASCADE', onUpdate: 'CASCADE' }))
  tema_id!: number;

  @BelongsTo({ target: () => PalavraChave, foreignKey: 'palavra_chave_id' })
  palavraChave!: BelongsToReference<PalavraChave>;

  @BelongsTo({ target: () => Tema, foreignKey: 'tema_id' })
  tema!: BelongsToReference<Tema>;

}
