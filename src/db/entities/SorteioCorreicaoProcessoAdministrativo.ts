// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection } from 'metal-orm';
import { Estado } from './Estado.js';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';
import { SorteioCorreicao } from './SorteioCorreicao.js';
import { SorteioCorreicaoProcessoAdministrativoQuesito } from './SorteioCorreicaoProcessoAdministrativoQuesito.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'sorteio_correicao_processo_administrativo' })
export class SorteioCorreicaoProcessoAdministrativo {
  /**
   * Identificador do sorteio
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador do sorteio
   * @remarks References dbo.sorteio_correicao.id (on delete CASCADE, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'sorteio_correicao', column: 'id', onDelete: 'CASCADE', onUpdate: 'NO ACTION' }))
  sorteio_correicao_id!: number;

  /**
   * Identificador do processo administrativo
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id!: number;

  /**
   * Observação sobre a  correição do processo administrativo
   */
  @Column(col.varchar(255))
  observacao?: string;

  /**
   * Identificador do avaliador
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  avaliador_id?: number;

  /**
   * Identificador do estado
   * @remarks References dbo.estado.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'estado', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  estado_id!: number;

  /**
   * Dados sobre o processo sorteado\nJSON com os dados do PA
   */
  @Column(col.text())
  dados_processo_sorteado?: string;

  @BelongsTo({ target: () => SorteioCorreicao, foreignKey: 'sorteio_correicao_id' })
  sorteioCorreicao?: SorteioCorreicao;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo?: ProcessoAdministrativo;

  @BelongsTo({ target: () => Usuario, foreignKey: 'avaliador_id' })
  avaliador?: Usuario;

  @BelongsTo({ target: () => Estado, foreignKey: 'estado_id' })
  estado?: Estado;

  @HasMany({ target: () => SorteioCorreicaoProcessoAdministrativoQuesito, foreignKey: 'sorteio_correicao_processo_administrativo_id' })
  sorteioCorreicaoProcessoAdministrativoQuesitos!: HasManyCollection<SorteioCorreicaoProcessoAdministrativoQuesito>;

}
