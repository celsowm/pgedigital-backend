// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection } from 'metal-orm';
import { Entrega } from './Entrega.js';
import { ManifestacaoProcessual } from './ManifestacaoProcessual.js';
import { Protocolizacao } from './Protocolizacao.js';
import { ReciboTj } from './ReciboTj.js';

@Entity({ tableName: 'resposta_tj' })
export class RespostaTj {
  /**
   * Identificador da resposta do TJ
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Quando se obtem êxito na resposta
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.boolean(), '((0))'))
  sucesso?: boolean;

  /**
   * Irá trazer a resposta do TJ
   */
  @Column(col.text())
  mensagem?: string;

  /**
   * Protocolo é um conjunto de informações, decisões, normas e de regras definidas a partir de um ato oficial
   */
  @Column(col.varchar(50))
  protocolo?: string;

  /**
   * Data no qual ocorre ou ocorreu a operação
   */
  @Column(col.datetime<Date>())
  data_operacao?: Date;

  /**
   * Trata da padronização do número dos processos no âmbito do Judiciário, com o intuito de facilitar o acesso às informações processuais e de agilizar a prestação jurisdicional.
   */
  @Column(col.varchar(20))
  cnj?: string;

  /**
   * Orgão no qual foi destinado
   */
  @Column(col.text())
  orgao_destino?: string;

  /**
   * Refere-se ao identificador da entrega da mensagem
   * @remarks References dbo.entrega.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'entrega', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  entrega_id?: number;

  /**
   * Refere-se ao id da manifestação processual
   * @remarks References dbo.manifestacao_processual.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'manifestacao_processual', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  manifestacao_processual_id?: number;

  /**
   * Refere-se a protocolização interna
   */
  @Column(col.varchar(50))
  prot_inter?: string;

  /**
   * Refere-se ao id da protocolização
   * @remarks References dbo.protocolizacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'protocolizacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  protocolizacao_id?: number;

  @HasMany({ target: () => ReciboTj, foreignKey: 'resposta_tj_id' })
  reciboTjs!: HasManyCollection<ReciboTj>;

  @BelongsTo({ target: () => Entrega, foreignKey: 'entrega_id' })
  entrega?: Entrega;

  @BelongsTo({ target: () => ManifestacaoProcessual, foreignKey: 'manifestacao_processual_id' })
  manifestacaoProcessual?: ManifestacaoProcessual;

  @BelongsTo({ target: () => Protocolizacao, foreignKey: 'protocolizacao_id' })
  protocolizacao?: Protocolizacao;

}
