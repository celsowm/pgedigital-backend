// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { EspecializadaTema } from './EspecializadaTema.js';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';

@Entity({ tableName: 'exito_sucumbencia' })
export class ExitoSucumbencia {
  /**
   * Identificador do êxito e sucumbência
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena a descrição de um resultado específico relacionado a uma situação de sucesso ou insucesso em um contexto legal ou administrativo.
   */
  @Column(col.notNull(col.text()))
  nome!: string;

  @HasMany({ target: () => EspecializadaTema, foreignKey: 'exito_sucumbencia_id' })
  especializadasTemas!: HasManyCollection<EspecializadaTema>;

  @HasMany({ target: () => ProcessoAdministrativo, foreignKey: 'exito_sucumbencia_id' })
  processosAdministrativos!: HasManyCollection<ProcessoAdministrativo>;

}
