// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection, BelongsToReference } from 'metal-orm';
import { Acervo } from './Acervo.js';
import { Tema } from './Tema.js';
import { TramitacaoAutomaticaAcervoTema } from './TramitacaoAutomaticaAcervoTema.js';

@Entity({ tableName: 'acervo_tema' })
export class AcervoTema {
  /**
   * Identificador único do relacionamento da tabela acervo com tema.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Tema que está relacionado.
   * @remarks References dbo.tema.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tema', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tema_id!: number;

  /**
   * Informa se está em distribuição automática.
   */
  @Column(col.boolean())
  distribuicao_automatica?: boolean;

  /**
   * Identificador do acervo em relação.
   * @remarks References dbo.acervo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'acervo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  acervo_id!: number;

  @BelongsTo({ target: () => Tema, foreignKey: 'tema_id' })
  tema!: BelongsToReference<Tema>;

  @BelongsTo({ target: () => Acervo, foreignKey: 'acervo_id' })
  acervo!: BelongsToReference<Acervo>;

  @HasMany({ target: () => TramitacaoAutomaticaAcervoTema, foreignKey: 'acervo_tema_id' })
  tramitacoesAutomaticaAcervoTema!: HasManyCollection<TramitacaoAutomaticaAcervoTema>;

}
