// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { AvisoPendente } from './AvisoPendente.js';
import { ComunicacaoEstado } from './ComunicacaoEstado.js';
import { ComunicacaoOutroParametro } from './ComunicacaoOutroParametro.js';
import { DecisaoJudicial } from './DecisaoJudicial.js';
import { DispensaGenerica } from './DispensaGenerica.js';
import { DocumentoComunicacao } from './DocumentoComunicacao.js';
import { Estado } from './Estado.js';
import { NivelSigilo } from './NivelSigilo.js';
import { OutroParametroComunicacao } from './OutroParametroComunicacao.js';
import { ProcessoJudicial } from './ProcessoJudicial.js';
import { ProvidenciaJuridica } from './ProvidenciaJuridica.js';
import { RegistroEnvioComunicacao } from './RegistroEnvioComunicacao.js';
import { TipoComunicacao } from './TipoComunicacao.js';
import { TipoDispensa } from './TipoDispensa.js';
import { TipoPrazo } from './TipoPrazo.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'comunicacao' })
export class Comunicacao {
  /**
   * Identificador único da comunicação.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Informa o número do processo judicial.
   */
  @Column(col.varchar(20))
  processo?: string;

  /**
   * Informa teor.
   */
  @Column(col.text())
  teor?: string;

  /**
   * Identificador que informa o tipo de comunicação.
   * @remarks References dbo.tipo_comunicacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.varchar(3), { table: 'tipo_comunicacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_comunicacao_id?: string;

  /**
   * Identificador que informa o aviso pendente.
   * @remarks References dbo.aviso_pendente.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'aviso_pendente', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  aviso_pendente_id?: number;

  /**
   * Informa o identificador do tipo do prazo
   * @remarks References dbo.tipo_prazo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.varchar(10), { table: 'tipo_prazo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tipo_prazo_id?: string;

  /**
   * Data de referência da comunicação.
   */
  @Column(col.datetime<Date>())
  data_referencia?: Date;

  /**
   * Informa se há prazo.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.int(), '((0))'))
  prazo?: number;

  /**
   * Identificador que informa o nível de sigilo.
   * @remarks References dbo.nivel_sigilo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'nivel_sigilo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  nivel_sigilo_id?: number;

  /**
   * Mensagem retornada.
   */
  @Column(col.text())
  mensagem?: string;

  /**
   * Retorna se houve sucesso.
   */
  @Column(col.boolean())
  sucesso?: boolean;

  /**
   * Identificador do responsável pela comunicação.
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  responsavel_id?: number;

  /**
   * Informa se é comunicação física.
   */
  @Column(col.boolean())
  comunicacao_fisica?: boolean;

  /**
   * Identificador do processo judicial vinculado.
   * @remarks References dbo.processo_judicial.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'processo_judicial', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_judicial_id?: number;

  /**
   * Informa o número de registro.
   */
  @Column(col.varchar(500))
  numero_registro?: string;

  /**
   * Informa a data do recebimento da comunicação.
   */
  @Column(col.datetime<Date>())
  data_recebimento?: Date;

  /**
   * Informa a chave identificadora do processo administrativo.
   */
  @Column(col.int())
  processo_administrativo_id?: number;

  /**
   * Informa o prazo de atendimento em hora
   */
  @Column(col.int())
  prazo_atendimento_hora?: number;

  /**
   * Informa o prazo de atendimento em dia
   */
  @Column(col.int())
  prazo_atendimento_dia?: number;

  /**
   * Identificador único do estado
   * @remarks References dbo.estado.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'estado', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  estado_id?: number;

  /**
   * Informa a competência
   */
  @Column(col.int())
  competencia?: number;

  /**
   * Informa a classe processual.
   */
  @Column(col.int())
  classe_processual?: number;

  /**
   * Informa o código do órgão.
   */
  @Column(col.int())
  codigo_orgao?: number;

  /**
   * Informa o nome do orgão.
   */
  @Column(col.varchar(500))
  nome_orgao?: string;

  /**
   * Informa o código da localidade.
   */
  @Column(col.int())
  codigo_localidade?: number;

  @Column(col.varchar(30))
  numero_comunicacao?: string;

  @BelongsToMany({ target: () => TipoDispensa, pivotTable: () => DispensaGenerica, pivotForeignKeyToRoot: 'comunicacao_id', pivotForeignKeyToTarget: 'tipo_dispensa_id' })
  tiposDispensas!: ManyToManyCollection<TipoDispensa>;

  @BelongsToMany({ target: () => DocumentoComunicacao, pivotTable: () => DocumentoComunicacao, pivotForeignKeyToRoot: 'comunicacao_id', pivotForeignKeyToTarget: 'parent_id' })
  documentosComunicacoes!: ManyToManyCollection<DocumentoComunicacao>;

  @BelongsToMany({ target: () => AvisoPendente, pivotTable: () => RegistroEnvioComunicacao, pivotForeignKeyToRoot: 'comunicacao_id', pivotForeignKeyToTarget: 'aviso_pendente_id' })
  avisosPendentes!: ManyToManyCollection<AvisoPendente>;

  @HasMany({ target: () => DispensaGenerica, foreignKey: 'comunicacao_id' })
  dispensasGenericas!: HasManyCollection<DispensaGenerica>;

  @HasMany({ target: () => ComunicacaoOutroParametro, foreignKey: 'comunicacao_id' })
  comunicacoesOutrosParametros!: HasManyCollection<ComunicacaoOutroParametro>;

  @HasMany({ target: () => OutroParametroComunicacao, foreignKey: 'comunicacao_id' })
  outrosParametrosComunicacoes!: HasManyCollection<OutroParametroComunicacao>;

  @HasMany({ target: () => DecisaoJudicial, foreignKey: 'comunicacao_id' })
  decisoesJudiciais!: HasManyCollection<DecisaoJudicial>;

  @HasMany({ target: () => ProvidenciaJuridica, foreignKey: 'comunicacao_id' })
  providenciasJuridicas!: HasManyCollection<ProvidenciaJuridica>;

  @BelongsTo({ target: () => TipoComunicacao, foreignKey: 'tipo_comunicacao_id' })
  tipoComunicacao!: BelongsToReference<TipoComunicacao>;

  @BelongsTo({ target: () => AvisoPendente, foreignKey: 'aviso_pendente_id' })
  avisoPendente!: BelongsToReference<AvisoPendente>;

  @BelongsTo({ target: () => TipoPrazo, foreignKey: 'tipo_prazo_id' })
  tipoPrazo!: BelongsToReference<TipoPrazo>;

  @BelongsTo({ target: () => NivelSigilo, foreignKey: 'nivel_sigilo_id' })
  nivelSigilo!: BelongsToReference<NivelSigilo>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'responsavel_id' })
  responsavel!: BelongsToReference<Usuario>;

  @BelongsTo({ target: () => ProcessoJudicial, foreignKey: 'processo_judicial_id' })
  processoJudicial!: BelongsToReference<ProcessoJudicial>;

  @BelongsTo({ target: () => Estado, foreignKey: 'estado_id' })
  estado!: BelongsToReference<Estado>;

  @HasMany({ target: () => ComunicacaoEstado, foreignKey: 'comunicacao_id' })
  comunicacoesEstados!: HasManyCollection<ComunicacaoEstado>;

  @HasMany({ target: () => RegistroEnvioComunicacao, foreignKey: 'comunicacao_id' })
  registrosEnviosComunicacoes!: HasManyCollection<RegistroEnvioComunicacao>;

}
