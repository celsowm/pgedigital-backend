// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey } from 'metal-orm';

@Entity({ tableName: 'classe_processual' })
export class ClasseProcessual {
  /**
   * Identificador único.
   */
  @PrimaryKey(col.notNull(col.int()))
  id!: number;

  /**
   * Armazena um identificador ou código que estabelece uma relação hierárquica entre diferentes classes processuais.
   */
  @Column(col.int())
  parent_id?: number;

  /**
   * Armazena informações que descrevem o tipo de item ou categoria associada a uma classe processual.
   */
  @Column(col.varchar(1))
  tipo_item?: string;

  /**
   * Armazena o nome ou título descritivo de uma classe processual.
   */
  @Column(col.varchar(500))
  nome?: string;

  /**
   * Armazena informações sobre o estado ou condição atual de uma classe processual.
   */
  @Column(col.varchar(1))
  situacao?: string;

  /**
   * Armazena a data em que uma classe processual específica foi incluída ou registrada no sistema.
   */
  @Column(col.date<Date>())
  data_inclusao?: Date;

  /**
   * Armazena informações sobre o usuário que incluiu ou registrou uma classe processual no sistema.
   */
  @Column(col.varchar(30))
  usuario_inclusao?: string;

  /**
   * Armazena a data em que ocorreu a última alteração em uma classe processual específica.
   */
  @Column(col.datetime<Date>())
  data_alteracao?: Date;

  /**
   * Armazena informações sobre o usuário que realizou a última alteração em uma classe processual específica.
   */
  @Column(col.varchar(30))
  usuario_alteracao?: string;

}
