// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, HasManyCollection, BelongsToReference } from 'metal-orm';
import { RespostaTj } from './RespostaTj.js';

@Entity({ tableName: 'manifestacao_processual' })
export class ManifestacaoProcessual {
  /**
   * Identificador da manifestação processual
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena o nome de arquivo associado a uma manifestação processual específica.
   */
  @Column(col.text())
  nome_arquivo?: string;

  /**
   * Identificador do tipo de documento
   */
  @Column(col.int())
  tipo_documento_id?: number;

  /**
   * Informa a manifestação processual pai na árvore de manifestação processual
   * @remarks References dbo.manifestacao_processual.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'manifestacao_processual', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  parent_id?: number;

  /**
   * É a parte que está se comunicando com o tribunal
   */
  @Column(col.text())
  autor_ldap?: string;

  /**
   * Cnj da manifestação processual
   */
  @Column(col.varchar(20))
  cnj?: string;

  /**
   * Data que a manifestação é protocolada
   */
  @Column(col.datetime<Date>())
  data_entrada?: Date;

  @HasMany({ target: () => RespostaTj, foreignKey: 'manifestacao_processual_id' })
  respostasTjs!: HasManyCollection<RespostaTj>;

  @BelongsTo({ target: () => ManifestacaoProcessual, foreignKey: 'parent_id' })
  parent!: BelongsToReference<ManifestacaoProcessual>;

  @HasMany({ target: () => ManifestacaoProcessual, foreignKey: 'parent_id' })
  manifestacoesProcessuais!: HasManyCollection<ManifestacaoProcessual>;

}
