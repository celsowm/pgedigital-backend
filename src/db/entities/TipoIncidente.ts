// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsToMany, HasManyCollection, ManyToManyCollection } from 'metal-orm';
import { DesabilitaTipoIntegracao } from './DesabilitaTipoIntegracao.js';
import { Incidente } from './Incidente.js';

@Entity({ tableName: 'tipo_incidente' })
export class TipoIncidente {
  /**
   * Refere-se ao tipo do incidente
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Um problema é "uma causa ou uma possível causa, de um ou mais incidentes"
   */
  @Column(col.notNull(col.text()))
  problema!: string;

  /**
   * Um incidente é uma circunstância acidental (ou que acontece por acaso) ou um episódio que acontece no decorrer de um acontecimento principal
   */
  @Column(col.text())
  descricao?: string;

  @BelongsToMany({ target: () => DesabilitaTipoIntegracao, pivotTable: () => Incidente, pivotForeignKeyToRoot: 'tipo_incidente_id', pivotForeignKeyToTarget: 'desabilita_tipo_integracao_id' })
  desabilitasTipoIntegracao!: ManyToManyCollection<DesabilitaTipoIntegracao>;

  @HasMany({ target: () => Incidente, foreignKey: 'tipo_incidente_id' })
  incidentes!: HasManyCollection<Incidente>;

}
