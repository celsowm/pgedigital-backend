// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, HasManyCollection } from 'metal-orm';
import { FatorCorrecao } from './FatorCorrecao.js';

@Entity({ tableName: 'indexador' })
export class Indexador {
  /**
   * Identificador do indexador
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Armazena o nome de um indexador específico.
   */
  @Column(col.notNull(col.varchar(250)))
  nome!: string;

  /**
   * Registra a data e hora em que um indexador específico foi criado no sistema.
   */
  @Column(col.datetime<Date>())
  created?: Date;

  /**
   * Registra a data e hora da última modificação ou atualização de um indexador específico no sistema.
   */
  @Column(col.datetime<Date>())
  modified?: Date;

  /**
   * Armazena informações sobre a taxa de juros associada a um determinado indexador.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  taxa_juros!: boolean;

  /**
   * Armazena informações sobre a correção monetária associada a um determinado indexador.
   * @defaultValue ((0))
   */
  @Column(col.defaultRaw(col.notNull(col.boolean()), '((0))'))
  correcao_monetaria!: boolean;

  @HasMany({ target: () => FatorCorrecao, foreignKey: 'indexador_id' })
  fatorCorrecaos!: HasManyCollection<FatorCorrecao>;

}
