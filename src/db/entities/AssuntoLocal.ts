// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, HasMany, BelongsTo, BelongsToMany, HasManyCollection, BelongsToReference, ManyToManyCollection } from 'metal-orm';
import { AssuntoProcessoJudicial } from './AssuntoProcessoJudicial.js';
import { Especializada } from './Especializada.js';
import { MniTribunal } from './MniTribunal.js';
import { PredicaoAssuntoEspecializada } from './PredicaoAssuntoEspecializada.js';
import { ProcessoJudicial } from './ProcessoJudicial.js';

@Entity({ tableName: 'assunto_local' })
export class AssuntoLocal {
  /**
   * Identificador único.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador do tribunal vinculado.
   * @remarks References dbo.mni_tribunal.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'mni_tribunal', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tribunal_id?: number;

  /**
   * Identificador do local do tribunal.
   */
  @Column(col.int())
  id_local_tribunal?: number;

  /**
   * Descrição do assunto.
   */
  @Column(col.text())
  descricao?: string;

  /**
   * Código que informa o pai nacional que será vinculado.
   */
  @Column(col.int())
  codigo_pai_nacional?: number;

  /**
   * Informa o pai local que será vinculado.
   */
  @Column(col.int())
  paiLocal?: number;

  /**
   * Checkbox que informa se é nacional.
   */
  @Column(col.boolean())
  nacional?: boolean;

  @BelongsToMany({ target: () => Especializada, pivotTable: () => PredicaoAssuntoEspecializada, pivotForeignKeyToRoot: 'assunto_id', pivotForeignKeyToTarget: 'especializada_id' })
  especializadas!: ManyToManyCollection<Especializada>;

  @BelongsToMany({ target: () => ProcessoJudicial, pivotTable: () => AssuntoProcessoJudicial, pivotForeignKeyToRoot: 'assunto_local_id', pivotForeignKeyToTarget: 'processo_judicial_id' })
  processosJudicial!: ManyToManyCollection<ProcessoJudicial>;

  @HasMany({ target: () => PredicaoAssuntoEspecializada, foreignKey: 'assunto_id' })
  predicoesAssuntoEspecializada!: HasManyCollection<PredicaoAssuntoEspecializada>;

  @BelongsTo({ target: () => MniTribunal, foreignKey: 'tribunal_id' })
  tribunal!: BelongsToReference<MniTribunal>;

  @HasMany({ target: () => AssuntoProcessoJudicial, foreignKey: 'assunto_local_id' })
  assuntosProcessoJudicial!: HasManyCollection<AssuntoProcessoJudicial>;

}
