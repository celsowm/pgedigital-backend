// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo, BelongsToReference } from 'metal-orm';
import { Especializada } from './Especializada.js';
import { ProcessoAdministrativo } from './ProcessoAdministrativo.js';
import { Usuario } from './Usuario.js';

@Entity({ tableName: 'historico_distribuicao_especializada' })
export class HistoricoDistribuicaoEspecializada {
  /**
   * Identificador do histórico de distribuição a especializada
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador do processo administrativo
   * @remarks References dbo.processo_administrativo.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'processo_administrativo', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  processo_administrativo_id!: number;

  /**
   * Identificador da especializada
   * @remarks References dbo.especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  especializada_id!: number;

  /**
   * Identificador do usuário da ação
   * @remarks References dbo.usuario.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'usuario', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  usuario_acao_id?: number;

  /**
   * Registra o horário exato em que a distribuição para a especializada ocorreu.
   */
  @Column(col.notNull(col.datetime<Date>()))
  horario_distribuicao!: Date;

  /**
   * Registra o endereço IP associado ao evento de distribuição a uma especializada.
   */
  @Column(col.varchar(15))
  ip?: string;

  @BelongsTo({ target: () => ProcessoAdministrativo, foreignKey: 'processo_administrativo_id' })
  processoAdministrativo!: BelongsToReference<ProcessoAdministrativo>;

  @BelongsTo({ target: () => Especializada, foreignKey: 'especializada_id' })
  especializada!: BelongsToReference<Especializada>;

  @BelongsTo({ target: () => Usuario, foreignKey: 'usuario_acao_id' })
  usuarioAcao!: BelongsToReference<Usuario>;

}
