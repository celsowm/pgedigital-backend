// AUTO-GENERATED by scripts/generate-entities.mjs
// Regenerate after schema changes.
import { col, Entity, Column, PrimaryKey, BelongsTo } from 'metal-orm';
import { Classificacao } from './Classificacao.js';
import { Especializada } from './Especializada.js';
import { FilaCircular } from './FilaCircular.js';
import { Tema } from './Tema.js';

@Entity({ tableName: 'classificacao_especializada_tema' })
export class ClassificacaoEspecializadaTema {
  /**
   * Identifidor único que relaciona as tabelas, classificação, especializadas e tema.
   * @remarks Auto-increment identity column
   */
  @PrimaryKey(col.notNull(col.autoIncrement(col.int())))
  id!: number;

  /**
   * Identificador da especializada.
   * @remarks References dbo.especializada.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'especializada', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  especializada_id!: number;

  /**
   * Identificador do tema relacionado.
   * @remarks References dbo.tema.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.notNull(col.int()), { table: 'tema', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  tema_id!: number;

  /**
   * Identificador da classificação.
   * @remarks References dbo.classificacao.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'classificacao', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  classificacao_id?: number;

  /**
   * Informa se está em fila circular.
   * @remarks References dbo.fila_circular.id (on delete NO ACTION, on update NO ACTION)
   */
  @Column(col.references(col.int(), { table: 'fila_circular', column: 'id', onDelete: 'NO ACTION', onUpdate: 'NO ACTION' }))
  fila_circular_id?: number;

  @BelongsTo({ target: () => Especializada, foreignKey: 'especializada_id' })
  especializada?: Especializada;

  @BelongsTo({ target: () => Tema, foreignKey: 'tema_id' })
  tema?: Tema;

  @BelongsTo({ target: () => Classificacao, foreignKey: 'classificacao_id' })
  classificacao?: Classificacao;

  @BelongsTo({ target: () => FilaCircular, foreignKey: 'fila_circular_id' })
  filaCircular?: FilaCircular;

}
